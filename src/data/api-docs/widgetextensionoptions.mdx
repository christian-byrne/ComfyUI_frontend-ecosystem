---
title: "WidgetExtensionOptions"
description: "Options object for defineWidgetExtension — custom widget rendering."
---

Defined in: [src/extension-api/types.ts:144](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/types.ts#L144)

Options for `defineWidget`. Describes an extension that provides a
custom widget type with its own DOM rendering.

## Stability

experimental

```ts
import { defineWidget } from '@comfyorg/extension-api'

export default defineWidget({
  name: 'my-org.color-picker',
  type: 'COLOR_PICKER',

  created(widget, node) {
    return {
      // mount color picker DOM
      render(container) {},
      // cleanup
      destroy() {}
    }
  }
})
```

## Properties

| Property | Type | Description |
| ------ | ------ | ------ |
| <a id="name"></a> `name` | `string` | Globally unique extension name. |
| <a id="type"></a> `type` | `string` | Widget type string this extension provides (e.g. `'COLOR_PICKER'`). |

## Methods

### created()?

```ts
optional created(widget, parentNode): 
  | void
  | {
  destroy?: void;
  render: void;
};
```

Defined in: [src/extension-api/types.ts:156](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/types.ts#L156)

Called once per widget instance. Return a `{ render, destroy }` pair for
custom DOM rendering, or `void` for non-visual widgets.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `widget` | [`WidgetHandle`](./widgethandle) |
| `parentNode` | [`NodeHandle`](./nodehandle) \| `null` |

#### Returns

  \| `void`
  \| \{
  `destroy?`: `void`;
  `render`: `void`;
\}

#### Stability

experimental

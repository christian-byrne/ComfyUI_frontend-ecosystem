---
title: "WidgetMountContext"
description: "API reference for WidgetMountContext."
---

Defined in: [src/extension-api/widget.ts:584](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/widget.ts#L584)

Context passed to a widget's `mount()` function.

Per **Axiom A12** (Mount-Lifecycle as the Sole DOM Seam), this is the only
surface through which DOM enters a widget. Authors capture the host element
and any constructed DOM via closure inside `mount()` — there is no
`widget.element` / `widget.inputEl` accessor on the handle.

## Stability

experimental

## Properties

| Property | Modifier | Type | Description |
| ------ | ------ | ------ | ------ |
| <a id="node"></a> `node` | `readonly` | [`NodeHandle`](./nodehandle) | The node hosting this widget. |
| <a id="widget"></a> `widget` | `readonly` | [`WidgetHandle`](./widgethandle) | The widget being mounted. Use for `getValue` / `setValue` / `on(...)`. |

## Methods

### onAfterRemount()

```ts
onAfterRemount(fn): void;
```

Defined in: [src/extension-api/widget.ts:614](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/widget.ts#L614)

Register a callback that fires immediately **after** the widget's host
`<div>` has been moved to a new location. Receives the new host element
so authors can re-attach observers, restore scroll position, etc.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | (`newHost`) => `void` |

#### Returns

`void`

***

### onBeforeRemount()

```ts
onBeforeRemount(fn): void;
```

Defined in: [src/extension-api/widget.ts:607](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/widget.ts#L607)

Register a callback that fires immediately **before** the widget's host
`<div>` is moved to a new location (graph↔app mode, subgraph promotion,
Vue `<KeepAlive>` shuffle). Use to detach observers, pause animations,
or capture scroll position before the move.

The widget's mount body is NOT re-invoked across a remount; only
`onBeforeRemount` then `onAfterRemount` fire.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `void` |

#### Returns

`void`

***

### onUnmount()

```ts
onUnmount(fn): void;
```

Defined in: [src/extension-api/widget.ts:596](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/widget.ts#L596)

Register a callback that fires when the widget entity is destroyed.
Equivalent to returning a cleanup function from `mount()`; provided as
a hook for composition (e.g. inside helpers that own their own
sub-resources).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `void` |

#### Returns

`void`

---
title: "NodeBeforeSerializeEvent"
description: "Pre-serialization hook payload — override or skip node data."
---

Defined in: [extension-api/node.ts:98](https://github.com/Comfy-Org/ComfyUI_frontend/blob/b87f7ea06725d1939dfa675334f7394cb9a5c0d2/src/extension-api/node.ts#L98)

Payload for `node.on('beforeSerialize', handler)`.

## Deprecated

Node-level serialization control will be removed in v1.0.
Use widget-level `widget.on('beforeSerialize')` instead. Store extension
state in widgets rather than arbitrary node fields.

**Why widget-level is better:**
- Widget values are visible at predictable locations in workflow JSON
- Cleaner separation between framework and extension concerns
- Widget serialization hooks support async operations

See ADR-0010 for full migration guidance.

## Stability

experimental

## Properties

| Property | Modifier | Type | Description |
| ------ | ------ | ------ | ------ |
| <a id="context"></a> ~~`context`~~ | `readonly` | `"workflow"` \| `"prompt"` \| `"clone"` \| `"subgraph-promote"` | Which serialization path triggered this. |
| <a id="data"></a> ~~`data`~~ | `readonly` | `Record`\<`string`, `unknown`\> | The mutable serialized node object. Mutate in place to append fields. Type intentionally loose — the exact shape is `ISerialisedNode`. |

## Methods

### ~~replace()~~

```ts
replace(fn): void;
```

Defined in: [extension-api/node.ts:114](https://github.com/Comfy-Org/ComfyUI_frontend/blob/b87f7ea06725d1939dfa675334f7394cb9a5c0d2/src/extension-api/node.ts#L114)

Replace the serialized object by providing a transform function.
`fn` receives the current `data` and should return the replacement.
Calling this multiple times chains: each call's `fn` receives the
previous call's output.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | (`orig`) => `Record`\<`string`, `unknown`\> |

#### Returns

`void`

---
title: "NodeExecutedEvent"
description: "Payload fired when a node finishes execution."
---

Defined in: [extension-api/node.ts:66](https://github.com/Comfy-Org/ComfyUI_frontend/blob/b87f7ea06725d1939dfa675334f7394cb9a5c0d2/src/extension-api/node.ts#L66)

Payload for `node.on('executed', handler)`.

Replaces the v1 `nodeType.prototype.onExecuted` patching pattern.

```ts
node.on('executed', (e) => {
  const text = e.output['text'] as string[]
  previewWidget.setValue(text.join('\n'))
})
```

## Properties

| Property | Modifier | Type | Description |
| ------ | ------ | ------ | ------ |
| <a id="output"></a> `output` | `readonly` | `Record`\<`string`, `unknown`\> | The backend execution output for this node. Shape varies by node type. |

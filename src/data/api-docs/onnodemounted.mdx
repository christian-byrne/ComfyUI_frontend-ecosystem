---
title: "onNodeMounted"
sidebarTitle: "onNodeMounted"
description: "Implicit-context lifecycle hook: fires when a node is fully mounted."
---

```ts
function onNodeMounted(fn): void;
```

Defined in: [services/extension-api-service.ts:912](https://github.com/Comfy-Org/ComfyUI_frontend/blob/b87f7ea06725d1939dfa675334f7394cb9a5c0d2/src/services/extension-api-service.ts#L912)

Register a callback that fires after setup() completes for this node entity
(post-flush, still within the active EffectScope).
Must be called synchronously inside nodeCreated / loadedGraphNode (D10a).

## Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `void` |

## Returns

`void`

---
title: 'NodeHandle'
description: 'Controlled access to node state, mutations, slots, and events.'
icon: 'circle-nodes'
---

Defined in: [src/extension-api/node.ts:234](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L234)

Controlled surface for node access. Reads query the ECS World; writes
dispatch commands. Events are Vue-reactive watches on World components.

```ts
import { defineNode } from '@comfyorg/extension-api'

export default defineNode({
  name: 'my-size-enforcer',
  nodeTypes: ['MyCustomNode'],

  nodeCreated(node) {
    const [w, h] = node.getSize()
    node.setSize([Math.max(w, 300), Math.max(h, 200)])

    node.on('executed', (e) => {
      console.log('output:', e.output)
    })
  }
})
```

## Properties

| Property                             | Modifier   | Type     | Description                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------ | ---------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <a id="comfyclass"></a> `comfyClass` | `readonly` | `string` | The ComfyUI backend class name (e.g. `'KSampler'`). Equal to `type` for most nodes; differs for reroute/virtual nodes. Read-only invariant.                                                                                                                                                                                                                                                                                         |
| <a id="id"></a> `id`                 | `readonly` | `string` | Opaque identifier for this node. Stable for the lifetime of the node entity. Treat as a string token: do not parse, slice, or compare its internal structure. Use [NodeHandle.equals](#equals) to compare with another handle. **Remarks** Per D20, the underlying value is a branded `NodeEntityId` at runtime but is narrowed to `string` on the public surface so authors never need to import a brand to type a local variable. |
| <a id="type"></a> `type`             | `readonly` | `string` | The LiteGraph node type string (e.g. `'KSampler'`). Read-only invariant: set at construction, never changes.                                                                                                                                                                                                                                                                                                                        |

## Methods

### addDOMWidget()

```ts
addDOMWidget(opts): WidgetHandle;
```

Defined in: [src/extension-api/node.ts:413](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L413)

Adds a DOM-backed widget to this node.

Replaces the v1 `node.addDOMWidget(name, type, element, opts)` pattern.
The runtime automatically:

- Reserves node height for the element (via auto-computeSize integration).
- Removes the element from the DOM when the node is removed.
- Includes the widget in `NodeHandle.getWidgets()`.

Use `WidgetHandle.setHeight(px)` to resize the reservation after initial mount.

#### Parameters

| Parameter | Type                                     |
| --------- | ---------------------------------------- |
| `opts`    | [`DOMWidgetOptions`](./domwidgetoptions) |

#### Returns

[`WidgetHandle`](./widgethandle)

A `WidgetHandle` for the registered DOM widget.

#### Stability

experimental

---

### addWidget()

```ts
addWidget(
   type,
   name,
   defaultValue,
   options?): WidgetHandle;
```

Defined in: [src/extension-api/node.ts:389](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L389)

Adds a new widget to this node.

#### Parameters

| Parameter      | Type                                            | Description                                               |
| -------------- | ----------------------------------------------- | --------------------------------------------------------- |
| `type`         | `string`                                        | Widget type string (e.g. `'INT'`, `'STRING'`, `'COMBO'`). |
| `name`         | `string`                                        | Unique widget name on this node.                          |
| `defaultValue` | `unknown`                                       | Initial value.                                            |
| `options?`     | `Partial`\<[`WidgetOptions`](./widgetoptions)\> | Optional type-specific options.                           |

#### Returns

[`WidgetHandle`](./widgethandle)

The new `WidgetHandle`.

---

### equals()

```ts
equals(other): boolean;
```

Defined in: [src/extension-api/node.ts:256](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L256)

Returns `true` if `other` represents the same node entity as this one.
Equivalent to `this.id === other.id` but the canonical comparator —
prefer `equals` over manual string comparison so future changes to the
identity scheme remain transparent.

#### Parameters

| Parameter | Type         |
| --------- | ------------ |
| `other`   | `NodeHandle` |

#### Returns

`boolean`

---

### getMode()

```ts
getMode(): NodeMode;
```

Defined in: [src/extension-api/node.ts:325](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L325)

Returns the node's current execution mode.

#### Returns

[`NodeMode`](./nodemode)

---

### getPosition()

```ts
getPosition(): Point;
```

Defined in: [src/extension-api/node.ts:279](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L279)

Returns the node's current canvas position as `[x, y]`.

#### Returns

[`Point`](./point)

---

### getProperties()

```ts
getProperties(): Record<string, unknown>;
```

Defined in: [src/extension-api/node.ts:349](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L349)

Returns a copy of all per-node-instance properties.

#### Returns

`Record`\<`string`, `unknown`\>

---

### getProperty()

```ts
getProperty<T>(key): T | undefined;
```

Defined in: [src/extension-api/node.ts:343](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L343)

Returns a per-node-instance property by key.

In v2, prefer routing persistent state through widget values or
`beforeSerialize` events. `node.properties` is kept as a migration shim
for v1 extensions that used it for per-instance widget config (e.g. min/max).

#### Type Parameters

| Type Parameter | Default type |
| -------------- | ------------ |
| `T`            | `unknown`    |

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `key`     | `string` |

#### Returns

`T` \| `undefined`

---

### getSize()

```ts
getSize(): Size;
```

Defined in: [src/extension-api/node.ts:291](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L291)

Returns the node's current size as `[width, height]`.

#### Returns

[`Size`](./size)

---

### getTitle()

```ts
getTitle(): string;
```

Defined in: [src/extension-api/node.ts:305](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L305)

Returns the node's display title. Defaults to the node type string.

#### Returns

`string`

---

### getWidget()

```ts
getWidget(name):
  | WidgetHandle<WidgetValue>
  | undefined;
```

Defined in: [src/extension-api/node.ts:372](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L372)

Returns a `WidgetHandle` for the named widget, or `undefined` if no such
widget exists on this node.

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `name`    | `string` |

#### Returns

\| [`WidgetHandle`](./widgethandle)\<[`WidgetValue`](./widgetvalue)\>
\| `undefined`

#### Example

```ts
const steps = node.getWidget('steps')
if (steps) steps.setValue(20)
```

---

### getWidgets()

```ts
getWidgets(): readonly WidgetHandle<WidgetValue>[];
```

Defined in: [src/extension-api/node.ts:378](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L378)

Returns all widgets on this node as `WidgetHandle` instances.

#### Returns

readonly [`WidgetHandle`](./widgethandle)\<[`WidgetValue`](./widgetvalue)\>[]

---

### inputs()

```ts
inputs(): readonly SlotInfo[];
```

Defined in: [src/extension-api/node.ts:421](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L421)

Returns all input slots on this node.

#### Returns

readonly [`SlotInfo`](./slotinfo)[]

---

### isSelected()

```ts
isSelected(): boolean;
```

Defined in: [src/extension-api/node.ts:317](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L317)

Returns `true` if the node is currently selected on the canvas.

#### Returns

`boolean`

---

### on()

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:440](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L440)

Subscribe to node removal (graph deletion, not subgraph promotion).

Replaces the v1 `nodeType.prototype.onRemoved` patching pattern.
Does NOT fire on subgraph promotion — the node's entity ID is preserved
across promotion.

##### Parameters

| Parameter | Type                             |
| --------- | -------------------------------- |
| `event`   | `"removed"`                      |
| `handler` | [`Handler`](./handler)\<`void`\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:450](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L450)

Subscribe to backend execution completion for this node.

Replaces the v1 `nodeType.prototype.onExecuted` patching pattern (the
most widely used anti-pattern per R4-P3; 5+ confirmed repos).

##### Parameters

| Parameter | Type                                                                 |
| --------- | -------------------------------------------------------------------- |
| `event`   | `"executed"`                                                         |
| `handler` | [`Handler`](./handler)\<[`NodeExecutedEvent`](./nodeexecutedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:460](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L460)

Subscribe to workflow hydration (node loaded from a saved workflow).

Replaces the v1 `nodeType.prototype.onConfigure` / `loadedGraphNode`
patterns. Fires after all widget values are restored from the workflow JSON.

##### Parameters

| Parameter | Type                             |
| --------- | -------------------------------- |
| `event`   | `"configured"`                   |
| `handler` | [`Handler`](./handler)\<`void`\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:471](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L471)

Subscribe to slot connection events.

Replaces `nodeType.prototype.onConnectInput`, `onConnectOutput`, and
`onConnectionsChange` patching patterns (R4-P4: six distinct signatures
in the wild — this single typed event resolves the confusion).

##### Parameters

| Parameter | Type                                                                   |
| --------- | ---------------------------------------------------------------------- |
| `event`   | `"connected"`                                                          |
| `handler` | [`Handler`](./handler)\<[`NodeConnectedEvent`](./nodeconnectedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:478](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L478)

Subscribe to slot disconnection events.

##### Parameters

| Parameter | Type                                                                         |
| --------- | ---------------------------------------------------------------------------- |
| `event`   | `"disconnected"`                                                             |
| `handler` | [`Handler`](./handler)\<[`NodeDisconnectedEvent`](./nodedisconnectedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:488](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L488)

Subscribe to canvas position changes.

##### Parameters

| Parameter | Type                                                                               |
| --------- | ---------------------------------------------------------------------------------- |
| `event`   | `"positionChanged"`                                                                |
| `handler` | [`Handler`](./handler)\<[`NodePositionChangedEvent`](./nodepositionchangedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:498](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L498)

Subscribe to node size changes.

##### Parameters

| Parameter | Type                                                                       |
| --------- | -------------------------------------------------------------------------- |
| `event`   | `"sizeChanged"`                                                            |
| `handler` | [`Handler`](./handler)\<[`NodeSizeChangedEvent`](./nodesizechangedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:505](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L505)

Subscribe to execution mode changes.

##### Parameters

| Parameter | Type                                                                       |
| --------- | -------------------------------------------------------------------------- |
| `event`   | `"modeChanged"`                                                            |
| `handler` | [`Handler`](./handler)\<[`NodeModeChangedEvent`](./nodemodechangedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:531](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L531)

Subscribe to node serialization. Async-capable.

##### Parameters

| Parameter | Type                                                                                         |
| --------- | -------------------------------------------------------------------------------------------- |
| `event`   | `"beforeSerialize"`                                                                          |
| `handler` | [`AsyncHandler`](./asynchandler)\<[`NodeBeforeSerializeEvent`](./nodebeforeserializeevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

##### Deprecated

Node-level serialization control will be removed in v1.0.
Use widget-level `widget.on('beforeSerialize')` instead — store extension
state in widgets rather than arbitrary node fields. See ADR-0010.

**Migration example:**

```ts
// BEFORE (deprecated)
node.on('beforeSerialize', (e) => {
  e.data['my_extension_state'] = computeState()
})

// AFTER (recommended)
const stateWidget = node.addWidget('STRING', '_my_state', '', { hidden: true })
stateWidget.on('beforeSerialize', (e) => {
  e.setSerializedValue(JSON.stringify(computeState()))
})
```

##### Stability

experimental

---

### outputs()

```ts
outputs(): readonly SlotInfo[];
```

Defined in: [src/extension-api/node.ts:427](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L427)

Returns all output slots on this node.

#### Returns

readonly [`SlotInfo`](./slotinfo)[]

---

### setMode()

```ts
setMode(mode): void;
```

Defined in: [src/extension-api/node.ts:331](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L331)

Sets the node's execution mode. Dispatches a `SetNodeMode` command.

#### Parameters

| Parameter | Type                     |
| --------- | ------------------------ |
| `mode`    | [`NodeMode`](./nodemode) |

#### Returns

`void`

---

### setPosition()

```ts
setPosition(pos): void;
```

Defined in: [src/extension-api/node.ts:285](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L285)

Moves the node to a new canvas position. Dispatches a `MoveNode` command.

#### Parameters

| Parameter | Type               |
| --------- | ------------------ |
| `pos`     | [`Point`](./point) |

#### Returns

`void`

---

### setProperty()

```ts
setProperty(key, value): void;
```

Defined in: [src/extension-api/node.ts:358](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L358)

Sets a per-node-instance property. Dispatches a `SetNodeProperty` command.

In v2, prefer `widget.setOption(key, value)` for widget-scoped per-instance
config (it persists to the `widget_options` sidecar in the workflow JSON).

#### Parameters

| Parameter | Type      |
| --------- | --------- |
| `key`     | `string`  |
| `value`   | `unknown` |

#### Returns

`void`

---

### setSize()

```ts
setSize(size): void;
```

Defined in: [src/extension-api/node.ts:297](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L297)

Resizes the node. Dispatches a `ResizeNode` command.

#### Parameters

| Parameter | Type             |
| --------- | ---------------- |
| `size`    | [`Size`](./size) |

#### Returns

`void`

---

### setTitle()

```ts
setTitle(title): void;
```

Defined in: [src/extension-api/node.ts:311](https://github.com/Comfy-Org/ComfyUI_frontend/blob/574a23209c2f8feec71375980a977e8d1fba89ec/src/extension-api/node.ts#L311)

Sets the node's display title. Dispatches a `SetNodeVisual` command.

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `title`   | `string` |

#### Returns

`void`

---
title: "NodeHandle"
description: "Controlled access to node state, mutations, slots, and events."
icon: "circle-nodes"
---

Defined in: [src/extension-api/node.ts:228](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L228)

Controlled surface for node access. Reads query the ECS World; writes
dispatch commands. Events are Vue-reactive watches on World components.

```ts
import { defineNode } from '@comfyorg/extension-api'

export default defineNode({
  name: 'my-size-enforcer',
  nodeTypes: ['MyCustomNode'],

  nodeCreated(node) {
    const [w, h] = node.getSize()
    node.setSize([Math.max(w, 300), Math.max(h, 200)])

    node.on('executed', (e) => {
      console.log('output:', e.output)
    })
  }
})
```

## Properties

| Property | Modifier | Type | Description |
| ------ | ------ | ------ | ------ |
| <a id="comfyclass"></a> `comfyClass` | `readonly` | `string` | The ComfyUI backend class name (e.g. `'KSampler'`). Equal to `type` for most nodes; differs for reroute/virtual nodes. Read-only invariant. |
| <a id="id"></a> `id` | `readonly` | `string` | Opaque identifier for this node. Stable for the lifetime of the node entity. Treat as a string token: do not parse, slice, or compare its internal structure. Use [NodeHandle.equals](#equals) to compare with another handle. **Remarks** Per D20, the underlying value is a branded `NodeEntityId` at runtime but is narrowed to `string` on the public surface so authors never need to import a brand to type a local variable. |
| <a id="type"></a> `type` | `readonly` | `string` | The LiteGraph node type string (e.g. `'KSampler'`). Read-only invariant: set at construction, never changes. |

## Methods

### addWidget()

```ts
addWidget(
   type, 
   name, 
   defaultValue, 
   options?): WidgetHandle;
```

Defined in: [src/extension-api/node.ts:400](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L400)

Adds a new widget to this node.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `type` | `string` | Widget type string (e.g. `'INT'`, `'STRING'`, `'COMBO'`). |
| `name` | `string` | Unique widget name on this node. |
| `defaultValue` | `unknown` | Initial value. |
| `options?` | `Partial`\<[`WidgetOptions`](./widgetoptions)\> | Optional type-specific options. |

#### Returns

[`WidgetHandle`](./widgethandle)

The new `WidgetHandle`.

***

### equals()

```ts
equals(other): boolean;
```

Defined in: [src/extension-api/node.ts:250](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L250)

Returns `true` if `other` represents the same node entity as this one.
Equivalent to `this.id === other.id` but the canonical comparator —
prefer `equals` over manual string comparison so future changes to the
identity scheme remain transparent.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | `NodeHandle` |

#### Returns

`boolean`

***

### getInputs()

```ts
getInputs(): readonly Readonly<SlotInfo>[];
```

Defined in: [src/extension-api/node.ts:436](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L436)

Returns all input slots on this node.

**Immutable view per D-immutability-enforcement (Hybrid C).** The returned
array and each slot are `Readonly` — `node.getInputs().push(...)`,
`node.getInputs()[i] = X`, and `node.getInputs()[i].name = "x"` all raise
TypeScript errors at compile time. Per-slot mutators (`setInputName`,
`replaceInput`, bulk field setters) are tracked under
W6.P8.UNMIGRATABLE / D-input-output-shape.

#### Returns

readonly `Readonly`\<[`SlotInfo`](./slotinfo)\>[]

#### Example

```ts
// ❌ TS-ERR — readonly array; v1 patterns no longer compile
node.getInputs().push({ name: 'x', type: 'INT' })
node.getInputs()[0].name = 'renamed'

// ✅ Read / iterate freely
const types = node.getInputs().map((s) => s.type)
```

***

### getMode()

```ts
getMode(): NodeMode;
```

Defined in: [src/extension-api/node.ts:319](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L319)

Returns the node's current execution mode.

#### Returns

[`NodeMode`](./nodemode)

***

### getOutputs()

```ts
getOutputs(): readonly Readonly<SlotInfo>[];
```

Defined in: [src/extension-api/node.ts:445](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L445)

Returns all output slots on this node.

**Immutable view per D-immutability-enforcement (Hybrid C).** Same
read-only semantics as [NodeHandle.getInputs](#getinputs). Per-slot mutators
tracked under W6.P8.UNMIGRATABLE / D-input-output-shape.

#### Returns

readonly `Readonly`\<[`SlotInfo`](./slotinfo)\>[]

***

### getPosition()

```ts
getPosition(): Point;
```

Defined in: [src/extension-api/node.ts:273](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L273)

Returns the node's current canvas position as `[x, y]`.

#### Returns

[`Point`](./point)

***

### getProperties()

```ts
getProperties(): Record<string, unknown>;
```

Defined in: [src/extension-api/node.ts:343](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L343)

Returns a copy of all per-node-instance properties.

#### Returns

`Record`\<`string`, `unknown`\>

***

### getProperty()

```ts
getProperty<T>(key): T | undefined;
```

Defined in: [src/extension-api/node.ts:337](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L337)

Returns a per-node-instance property by key.

In v2, prefer routing persistent state through widget values or
`beforeSerialize` events. `node.properties` is kept as a migration shim
for v1 extensions that used it for per-instance widget config (e.g. min/max).

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |

#### Returns

`T` \| `undefined`

***

### getSize()

```ts
getSize(): Size;
```

Defined in: [src/extension-api/node.ts:285](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L285)

Returns the node's current size as `[width, height]`.

#### Returns

[`Size`](./size)

***

### getTitle()

```ts
getTitle(): string;
```

Defined in: [src/extension-api/node.ts:299](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L299)

Returns the node's display title. Defaults to the node type string.

#### Returns

`string`

***

### getWidget()

```ts
getWidget(name): 
  | WidgetHandle<WidgetValue>
  | undefined;
```

Defined in: [src/extension-api/node.ts:366](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L366)

Returns a `WidgetHandle` for the named widget, or `undefined` if no such
widget exists on this node.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |

#### Returns

  \| [`WidgetHandle`](./widgethandle)\<[`WidgetValue`](./widgetvalue)\>
  \| `undefined`

#### Example

```ts
const steps = node.getWidget('steps')
if (steps) steps.setValue(20)
```

***

### getWidgets()

```ts
getWidgets(): readonly Readonly<WidgetHandle<WidgetValue>>[];
```

Defined in: [src/extension-api/node.ts:389](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L389)

Returns all widgets on this node as `WidgetHandle` instances.

**Immutable view per D-immutability-enforcement (Hybrid C).** The returned
array cannot be mutated (`push`, `splice`, `length =`, index assignment
all raise TS errors). Each `WidgetHandle` is also surface-frozen — use
the `WidgetHandle` setter methods (`setValue`, `setHidden`, etc.) to
mutate widget state. To add or remove widgets, use
[NodeHandle.addWidget](#addwidget) / future `removeWidget` (W6.P8.UNMIGRATABLE).

#### Returns

readonly `Readonly`\<[`WidgetHandle`](./widgethandle)\<[`WidgetValue`](./widgetvalue)\>\>[]

#### Example

```ts
// ❌ TS-ERR — readonly array; v1 patterns no longer compile
node.getWidgets().push(newWidget)
node.getWidgets()[0] = newWidget

// ✅ Iterate / read freely
for (const w of node.getWidgets()) console.log(w.name)
const labels = node.getWidgets().map((w) => w.label)
```

***

### ~~inputs()~~

```ts
inputs(): readonly Readonly<SlotInfo>[];
```

Defined in: [src/extension-api/node.ts:452](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L452)

#### Returns

readonly `Readonly`\<[`SlotInfo`](./slotinfo)\>[]

#### Deprecated

Use [NodeHandle.getInputs](#getinputs) instead. Renamed to align
with the `getX()` accessor convention (D11/D-immutability-enforcement).
Will be removed in v1.0.

***

### isSelected()

```ts
isSelected(): boolean;
```

Defined in: [src/extension-api/node.ts:311](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L311)

Returns `true` if the node is currently selected on the canvas.

#### Returns

`boolean`

***

### on()

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:472](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L472)

Subscribe to node removal (graph deletion, not subgraph promotion).

Replaces the v1 `nodeType.prototype.onRemoved` patching pattern.
Does NOT fire on subgraph promotion — the node's entity ID is preserved
across promotion.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"removed"` |
| `handler` | [`Handler`](./handler)\<`void`\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:482](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L482)

Subscribe to backend execution completion for this node.

Replaces the v1 `nodeType.prototype.onExecuted` patching pattern (the
most widely used anti-pattern per R4-P3; 5+ confirmed repos).

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"executed"` |
| `handler` | [`Handler`](./handler)\<[`NodeExecutedEvent`](./nodeexecutedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:492](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L492)

Subscribe to workflow hydration (node loaded from a saved workflow).

Replaces the v1 `nodeType.prototype.onConfigure` / `loadedGraphNode`
patterns. Fires after all widget values are restored from the workflow JSON.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"configured"` |
| `handler` | [`Handler`](./handler)\<`void`\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:503](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L503)

Subscribe to slot connection events.

Replaces `nodeType.prototype.onConnectInput`, `onConnectOutput`, and
`onConnectionsChange` patching patterns (R4-P4: six distinct signatures
in the wild — this single typed event resolves the confusion).

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"connected"` |
| `handler` | [`Handler`](./handler)\<[`NodeConnectedEvent`](./nodeconnectedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:510](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L510)

Subscribe to slot disconnection events.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"disconnected"` |
| `handler` | [`Handler`](./handler)\<[`NodeDisconnectedEvent`](./nodedisconnectedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:520](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L520)

Subscribe to canvas position changes.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"positionChanged"` |
| `handler` | [`Handler`](./handler)\<[`NodePositionChangedEvent`](./nodepositionchangedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:530](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L530)

Subscribe to node size changes.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"sizeChanged"` |
| `handler` | [`Handler`](./handler)\<[`NodeSizeChangedEvent`](./nodesizechangedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:537](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L537)

Subscribe to execution mode changes.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"modeChanged"` |
| `handler` | [`Handler`](./handler)\<[`NodeModeChangedEvent`](./nodemodechangedevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

#### Call Signature

```ts
on(event, handler): Unsubscribe;
```

Defined in: [src/extension-api/node.ts:563](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L563)

Subscribe to node serialization. Async-capable.

##### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `"beforeSerialize"` |
| `handler` | [`AsyncHandler`](./asynchandler)\<[`NodeBeforeSerializeEvent`](./nodebeforeserializeevent)\> |

##### Returns

[`Unsubscribe`](./unsubscribe)

A cleanup function to remove the listener.

##### Deprecated

Node-level serialization control will be removed in v1.0.
Use widget-level `widget.on('beforeSerialize')` instead — store extension
state in widgets rather than arbitrary node fields. See ADR-0010.

**Migration example:**
```ts
// BEFORE (deprecated)
node.on('beforeSerialize', (e) => {
  e.data['my_extension_state'] = computeState()
})

// AFTER (recommended)
const stateWidget = node.addWidget('STRING', '_my_state', '', { hidden: true })
stateWidget.on('beforeSerialize', (e) => {
  e.setSerializedValue(JSON.stringify(computeState()))
})
```

##### Stability

experimental

***

### ~~outputs()~~

```ts
outputs(): readonly Readonly<SlotInfo>[];
```

Defined in: [src/extension-api/node.ts:459](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L459)

#### Returns

readonly `Readonly`\<[`SlotInfo`](./slotinfo)\>[]

#### Deprecated

Use [NodeHandle.getOutputs](#getoutputs) instead. Renamed to align
with the `getX()` accessor convention (D11/D-immutability-enforcement).
Will be removed in v1.0.

***

### setMode()

```ts
setMode(mode): void;
```

Defined in: [src/extension-api/node.ts:325](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L325)

Sets the node's execution mode. Dispatches a `SetNodeMode` command.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `mode` | [`NodeMode`](./nodemode) |

#### Returns

`void`

***

### setPosition()

```ts
setPosition(pos): void;
```

Defined in: [src/extension-api/node.ts:279](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L279)

Moves the node to a new canvas position. Dispatches a `MoveNode` command.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pos` | [`Point`](./point) |

#### Returns

`void`

***

### setProperty()

```ts
setProperty(key, value): void;
```

Defined in: [src/extension-api/node.ts:352](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L352)

Sets a per-node-instance property. Dispatches a `SetNodeProperty` command.

In v2, prefer `widget.setOption(key, value)` for widget-scoped per-instance
config (it persists to the `widget_options` sidecar in the workflow JSON).

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |
| `value` | `unknown` |

#### Returns

`void`

***

### setSize()

```ts
setSize(size): void;
```

Defined in: [src/extension-api/node.ts:291](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L291)

Resizes the node. Dispatches a `ResizeNode` command.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `size` | [`Size`](./size) |

#### Returns

`void`

***

### setTitle()

```ts
setTitle(title): void;
```

Defined in: [src/extension-api/node.ts:305](https://github.com/Comfy-Org/ComfyUI_frontend/blob/719d193be0202984922f7cb01f9ef667d3729ea0/src/extension-api/node.ts#L305)

Sets the node's display title. Dispatches a `SetNodeVisual` command.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `title` | `string` |

#### Returns

`void`

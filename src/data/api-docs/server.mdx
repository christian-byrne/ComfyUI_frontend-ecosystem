---
title: "server"
description: "API reference for server."
---

```ts
const server: EventNamespace<ServerEventPayloads>;
```

Defined in: [extension-api/events.ts:200](https://github.com/Comfy-Org/ComfyUI_frontend/blob/b87f7ea06725d1939dfa675334f7394cb9a5c0d2/src/extension-api/events.ts#L200)

Non-execution backend events + custom-node events.

Canonical events: `'status'`, `'logs'`, `'reconnected'`, `'feature_flags'`,
`'assets'`. Custom-node events ride this channel with arbitrary string
(e.g. `server.on('rayko.inspline.show', ...)`). Module-augment
`ServerEventPayloads` to type custom events.

## Public API

## Stability

experimental
